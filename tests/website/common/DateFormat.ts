import moment = require('moment');

export const timestamp_now = moment()

export const formattedMTime_now = moment().format('YYYY/MM/DD HH:mm')

export const formattedSTime_now = moment().format('YYYY/MM/DD HH:mm:ss')

export const formattedDateTime_now = moment().format('YYYY/MM/DD')

export const formatted_now = moment().format('YYYYMMDDHHmmss')

