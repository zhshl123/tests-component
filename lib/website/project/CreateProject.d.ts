/**
 * 添加project
 */
export declare const addProject: {
    using: (projectInfo: DataTable) => any;
};
export declare const fillProjectRequiredFields: {
    using: (Project: string, Department: string, Program: string, Currency: string) => any;
};
